spring:
  application:
    name: nutonbe
  
  datasource:
    url: jdbc:h2:mem:nutonbedb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
welcome:
    message: Welcome test